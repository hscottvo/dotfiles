repos:
  - repo: local
    hooks:
      - id: nixfmt
        name: nixfmt
        entry: nixfmt --strict
        language: system
        files: \.nix$

      - id: stylua
        name: stylua
        entry: stylua
        language: system
        files: \.lua$
